{
  "$ref": "#/definitions/events",
  "definitions": {
    "events": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "startDate": {
          "anyOf": [
            {
              "type": "string",
              "format": "date-time"
            },
            {
              "type": "string",
              "format": "date"
            },
            {
              "type": "integer",
              "format": "unix-time"
            }
          ]
        },
        "startTime": {
          "type": "string"
        },
        "endDate": {
          "anyOf": [
            {
              "type": "string",
              "format": "date-time"
            },
            {
              "type": "string",
              "format": "date"
            },
            {
              "type": "integer",
              "format": "unix-time"
            }
          ]
        },
        "endTime": {
          "$ref": "#/definitions/events/properties/startTime"
        },
        "recurrence": {
          "type": "string",
          "enum": [
            "weekly",
            "biweekly",
            "monthly"
          ]
        },
        "recurrenceUntil": {
          "$ref": "#/definitions/events/properties/endDate"
        },
        "location": {
          "$ref": "#/definitions/events/properties/startTime"
        },
        "address": {
          "$ref": "#/definitions/events/properties/startTime"
        },
        "link": {
          "$ref": "#/definitions/events/properties/startTime"
        },
        "image": {
          "$ref": "#/definitions/events/properties/startTime"
        },
        "description": {
          "$ref": "#/definitions/events/properties/startTime"
        }
      },
      "required": [
        "name",
        "startDate"
      ],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}